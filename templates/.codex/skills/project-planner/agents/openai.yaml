interface:
  display_name: "Project Planner"
  short_description: "Project plans, task breakdowns, dependencies, and agent routing."
  brand_color: "#7C3AED"
  default_prompt: "$project-planner"

policy:
  allow_implicit_invocation: true
