interface:
  display_name: "Database Design"
  short_description: "Schema design, indexing, ORMs, and query optimization."
  brand_color: "#10B981"
  default_prompt: "$database-design"

policy:
  allow_implicit_invocation: true
