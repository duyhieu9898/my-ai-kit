interface:
  display_name: "Project Planner"
  short_description: "Roadmaps, dependencies, risks, and verification"
  brand_color: "#7C3AED"
  default_prompt: "Use $project-planner to create a repository-aware roadmap for this initiative."

policy:
  allow_implicit_invocation: true
